namespace Presentation.UI
{
    class Program
    {
        static void Main(string[] args)
        {
            var filesPath = args[0];
            //filesToParse = FileManager.DiscoverFiles(filesPath);
            //Console.log("Put your word here: ");
            //word = readfromconsole();
            //results = FileParser.findWordInFiles(filesToParse, word);
            //Print("results")
        }
    }
}
